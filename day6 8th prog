#include <stdio.h>
int main() 
{
 int num,n,i=1;
 printf(" Enter the Number:");
 scanf("%d", &num);
 printf("Enter the number upto be printed:");
 scanf("%d",&n);
 printf("Multiplication table of %d: ", num);
 while (i <= n)
{
 printf("\n%d x %d = %d", num, i, num * i);
 i++;
 }
 return 0;
}
